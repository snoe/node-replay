{
    "name": "replay",
    "version": "1.0.0",
    "description": "An http client that subscribes to a redis queue of requests and redirects them",
    "engines": {
        "node": ">= 0.4.1",
        "coffee", ">= 1.1.2"
    },
    "dependencies": {
       "coffee-script": ">= 1.1.2",
       "redis": ">= 0.6.7",
       "restler": ">= 0.2.1",
       "underscore": "1.2.0"
    },
    "scripts": { 
        "start": "coffee replay.coffee" 
    }
}
